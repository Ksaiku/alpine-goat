# ALPINE GOAT — 仕様書 v1.0

リアルなヤギを主人公にした、ステージクリア型の横スクロールアクションゲーム。

---

## 1. 基本情報

| 項目 | 内容 |
|---|---|
| 仮タイトル | ALPINE GOAT |
| ジャンル | ステージクリア型 横スクロールアクション |
| プラットフォーム | Web（GitHub Pages） |
| 対応環境 | PCブラウザ（キーボード）／スマートフォン（タッチ） |
| 技術 | Vanilla JavaScript (ES Modules) + Canvas 2D API |
| 外部ライブラリ | **一切使用しない**（ビルド不要・CDN不要） |
| 想定プレイ時間 | 全3ステージ 合計5〜8分 |

---

## 2. コアループ

**走る → 跳ぶ → 踏む → 突進する** の4動詞のみ。これ以外の動詞は v1 では追加しない。

「リアルなヤギ」は写実的な絵ではなく **挙動のリアルさ** で表現する。
具体的には、多関節の骨格アニメーション、体幹の傾き、慣性、着地時の沈み込み、頭部の前後振りで表現する。

---

## 3. 操作

| 動作 | キーボード | タッチ |
|---|---|---|
| 左右移動 | `←` `→` / `A` `D` | 画面左下の方向パッド |
| ジャンプ | `Space` / `↑` / `W` | 画面右下 A ボタン |
| 頭突き（ダッシュ突進） | `Shift` / `J` | 画面右下 B ボタン |
| ポーズ | `Esc` / `P` | 画面右上 ボタン |
| 決定（メニュー） | `Space` / `Enter` | タップ |

- ジャンプは**1段のみ**（二段ジャンプなし）。
- タッチUIはタッチ入力を検知したときだけ表示する（PCでは非表示）。

---

## 4. 座標系と解像度

- **論理解像度: 960 × 540 px 固定**。すべての座標・速度はこの論理空間で計算する。
- canvas の内部解像度は `960×540 × devicePixelRatio` とし、CSS でアスペクト比を維持して拡大（レターボックス）。
- 1タイル = **32px**。地形はタイル単位で設計する。
- Y軸は下向きが正。

---

## 5. 物理パラメータ（`src/config.js` に集約）

すべて px / 秒 の単位。**これらは仮の値であり、実装後に手触りを見て調整する前提。マジックナンバーをコード中に直接書かず、必ず config.js から参照すること。**

```js
GRAVITY:            2200   // px/s^2
MAX_FALL_SPEED:     1200
MOVE_SPEED:          260   // 最高水平速度
ACCEL_GROUND:       2000
ACCEL_AIR:          1200
FRICTION_GROUND:    2400
JUMP_VELOCITY:      -800   // ジャンプ初速。最高到達点 138.8px（4.34タイル）
JUMP_CUT_MULTIPLIER: 0.45  // ジャンプキーを離した瞬間、上昇速度に乗算
COYOTE_TIME:        0.09   // 秒。地面を離れてもジャンプを受け付ける猶予
JUMP_BUFFER_TIME:   0.12   // 秒。着地前のジャンプ入力を先行受付
DASH_SPEED:          620
DASH_DURATION:      0.26   // 秒
DASH_COOLDOWN:      0.60   // 秒
DASH_GRAVITY_SCALE: 0.35   // 突進中の重力倍率
STOMP_BOUNCE:       -520   // 踏みつけ成功時の跳ね返り
INVINCIBLE_TIME:     1.2   // 秒。リスポーン後の無敵時間
DEATH_ANIM_TIME:     0.6   // 秒。死亡演出の長さ
```

---

## 6. ヤギの状態機械

`idle` / `walk` / `run` / `jump` / `fall` / `dash` / `hurt` / `dead`

- 当たり判定は **40 × 48 px の矩形（AABB）**。描画サイズ（約64×56px）より小さくする。
- 描画の原点は当たり判定矩形の「足元中央」。

---

## 6.5 レベルデザインの基準値（実測・確定値）

固定タイムステップ 1/60 秒で実際に積分した結果の**実測値**。Phase 11 のレベルデザインはこの数値を根拠にすること。

| 項目 | 値 |
|---|---|
| ジャンプ最高到達点 | **138.8 px = 4.34 タイル** |
| 滞空時間（ジャンプ〜着地） | **0.72 秒** |
| 最高速で跳んだときの水平到達距離 | **186 px = 5.8 タイル** |

**レベルデザインの安全基準:**

| 設計対象 | 安全値 | 限界値 |
|---|---|---|
| 登れる段差の高さ | **3 タイル（96px）** | 4 タイル（128px）※助走2タイル以上が必要 |
| 跳び越せる穴の幅 | **4 タイル（128px）** | 5 タイル（160px）※最高速の助走が必須 |
| 天井までの必要な高さ | ヤギの身長48px + 通り抜け余裕16px = **2 タイル以上** | — |

> **★空中突進が無制限のため、「穴の幅」で難易度を作ってはならない。**
> 突進を連打すればどんな幅の穴も越えられる。**難易度は「高さ・敵の配置・棘」で作ること。**
> 穴は「落ちたら戻される」という緊張感の演出として置き、越えること自体を課題にしない。
>
> **一方、突進は高さを稼げない**（発動時に縦速度を0にし、水平にしか進まない）。
> したがって **「上へ登ること」は依然として有効な課題**である。難所は**縦方向**に作ること。
> 水平方向に長く飛ばせたくない区間は、**天井を低くして滑空を物理的に妨げる**。

> **注意:** 4タイルの段差は、壁の手前**約2タイル（65px）**の位置でジャンプしないと壁に阻まれる。
> 初心者が失敗しやすいため、**必修ルート上には置かず、任意の高所ルートにのみ使う。**

---

## 7. 攻撃1: 踏みつけ

**判定条件（両方を満たすこと）:**
1. ヤギの垂直速度 `vy > 0`（落下中）
2. ヤギの当たり判定の下端が、敵の当たり判定の上端から **16px 以内** の範囲で重なっている

**成功時:** 敵を撃破し、ヤギに `vy = STOMP_BOUNCE` を与える。

**それ以外の接触:** 被弾（＝即死）。

> **設計方針:** 1発死のため、踏みつけ判定は必ず寛容側に倒す。敵の当たり判定は見た目より上部を厚く取る。

---

## 8. 攻撃2: 頭突き（ダッシュ突進）

- 発動すると `DASH_DURATION` の間、向いている方向へ `DASH_SPEED` で直進する。
- 突進中は**水平入力を受け付けない**。重力は `DASH_GRAVITY_SCALE` 倍。
- 突進中は「攻撃状態」となり、敵に接触すると敵を撃破する（敵は吹き飛んで消滅）。
- **破壊可能な岩ブロック**を破壊できる。
- 突進中でも **棘と落下death は無効化されない**。
- 空中でも発動可能。着地または `DASH_DURATION` 経過で終了。
- **クールダウンは接地時のみ適用する（2026-09-07 の設計変更）。**

| 突進が終わった状況 | 次に撃てるまで |
|---|---|
| 地上で終了 / 空中で撃って着地 | `DASH_COOLDOWN`（0.60秒） |
| **空中で持続時間が尽きて終了** | **`DASH_AIR_COOLDOWN`（0秒）＝すぐ撃てる** |

> **意図:** 空中で突進を連打でき、滑空するように移動できる。操作の自由度と爽快感を優先した判断。
> 突進キーは押しっぱなしでは連射されない（押し直しが必要）ため、連続突進は意図的な操作でのみ成立する。

---

## 9. ダメージ処理（1発死）

**死因:** 敵への非攻撃接触 / 棘への接触 / 画面下端より下への落下

**フロー:**
1. 死亡 → `DEATH_ANIM_TIME` の演出（ヤギが上に跳ね、回転しながら落下。操作不能）
2. 直近の到達済みチェックポイントから再開
3. 復帰後 `INVINCIBLE_TIME` の間は無敵（点滅表示）

- **残機なし・無限リトライ。** ゲームオーバー画面は存在しない。
- ステージ内の死亡回数をカウントし、クリア画面に表示する。
- **チェックポイント:** 各ステージに3〜4個。**難所（連続ジャンプ・棘地帯・敵密集地）の直前に必ず配置する。** 到達すると旗が立つ演出。

---

## 10. 敵（3種）

| 名前 | 挙動 | 踏みつけ | 頭突き |
|---|---|---|---|
| マーモット | 地面を左右に往復。プラットフォームの端と壁で反転。速度 60px/s | 撃破 | 撃破 |
| ワシ | 空中を水平移動しながら上下にサインカーブ（振幅48px, 周期2秒）。速度 90px/s | 撃破 | 撃破 |
| ヤマアラシ | 地面を左右に往復。速度 45px/s。**背中に棘** | **死亡** | 撃破 |

> ヤマアラシは「踏むな、突進しろ」をプレイヤーに教えるための敵。Stage 3 で初登場させる。

---

## 11. 地形要素

| 要素 | 挙動 |
|---|---|
| 実体ブロック（岩） | 全方向から衝突する |
| すり抜け床 | 上から落下してくる時のみ着地する。下から突き抜けられる |
| 棘 | 接触で即死 |
| 破壊可能岩 | 頭突きで破壊。通常は実体ブロックと同じ |
| 落下穴 | 地形の欠落。画面下端より下に落ちると死亡 |
| ゴール旗 | 接触でステージクリア |
| チェックポイント旗 | 接触で復帰地点を更新 |

---

### タイル記号（ステージデータの表記）

ステージは「1文字＝1タイル」の文字列の配列で記述する。人間が直接読み書きして地形を設計できるようにするため。

| 記号 | 意味 | 導入フェーズ |
|---|---|---|
| `.` | 空（何もない） | Phase 4 |
| `#` | 実体ブロック（岩） | Phase 4 |
| `=` | すり抜け床 | Phase 4 |
| `^` | 棘 | Phase 4（判定のみ。死亡は Phase 9） |
| `*` | 破壊可能岩 | Phase 4（実体として。破壊は Phase 7） |
| `C` | チェックポイント | Phase 9 |
| `G` | ゴール旗 | Phase 10 |
| `M` | 敵：マーモット | Phase 8 |
| `E` | 敵：ワシ | Phase 8 |
| `Y` | 敵：ヤマアラシ | Phase 8 |

未実装の記号が現れても、パーサはエラーを出さず空タイルとして無視すること。

---

## 12. ステージ構成（全3ステージ）

**1ステージの規模（2026-09-07 に拡大）:**

| 項目 | 値 |
|---|---|
| 横幅 | **300タイル = 9600px** |
| 高さ | 24タイル = 768px |
| プレイ時間の目安 | **60〜120秒** |
| チェックポイント | **4個**（難所の直前に必ず置く） |
| 敵の数 | 8〜12体 |

> ステージが長いぶん、**同じ地形を続けないこと。** 30〜40タイルごとに主題を変える
> （平地 → 段差 → 穴 → すり抜け床 → 天井の低い回廊 → 階段の上り下り …）。


### Stage 1 「山麓の斜面」
基本操作の教育ステージ。難易度は最低。
- 序盤: 平坦な地面のみ（走ることを覚える）
- 中盤: 段差 → 落下穴 → マーモット1体（ジャンプと踏みつけを覚える）
- 終盤: 穴と敵の組み合わせ
- 登場敵: **マーモットのみ**
- 頭突きは登場させない（Stage 2 で導入）

### Stage 2 「氷穴」
縦方向の移動と、すり抜け床を導入。配色は暗く寒色。
- **すり抜け床**による上下移動の足場
- **ワシ**が初登場（空中の脅威）
- 頭突きを教えるための破壊可能岩の壁を1箇所配置

### Stage 3 「山頂への道」
全要素を使った総合ステージ。最高難度。
- **ヤマアラシ**初登場（踏むと死ぬ敵）
- 破壊可能岩、棘地帯、長い連続ジャンプ
- ゴールは山頂の旗

---

## 13. 背景（3層パララックス）

すべてプロシージャル描画。ステージごとに配色パレットを差し替える。

| 層 | 内容 | スクロール係数 |
|---|---|---|
| 遠景 | 空のグラデーション + 雪山の稜線 | 0.15 |
| 中景 | 針葉樹林のシルエット | 0.40 |
| 近景 | 岩肌のシルエット | 0.75 |

---

## 14. ヤギの描画仕様（プロシージャル）

**外部画像を一切使わず、Canvas の Path API のみで描画する。**

- 側面視点。**右向きを基準**とし、左向きは水平反転で描画する。
- **部位:** 胴体 / 首 / 頭 / 角×2 / 耳 / 4脚（各脚は 上腿・下腿・蹄 の2関節）/ 尾 / 顎髭
- **実装方式:** 骨格をローカル座標の関節角度として定義し、状態と時間から角度を計算 → その角度から各部位のパスを生成して描画する。（＝スプライトのコマ切り替えではなく、関節駆動）
- **質感:** 胴体に線形グラデーション（背中側を明るく、腹側を暗く）。輪郭に沿って短い毛のストロークを数十本描く。
- **描画サイズ:** 幅 約64px × 高さ 約56px
- **アニメ:** 走行時は4本の脚の位相をずらし（前脚と後脚で対角の位相）、胴体を上下に微振動させ、頭を前後に振る。着地時は脚を曲げて沈み込ませる。

---

## 15. 画面遷移

`タイトル` → `ステージ選択` → `プレイ` → `クリア画面` → `ステージ選択`

- ステージ選択では、クリア済みの次のステージまでが解放される。
- 進捗（クリア済ステージ、各ステージのベストタイムと最少死亡回数）は **localStorage** に保存する。
- **HUD:** 経過タイム / 死亡回数 / ステージ名

---

## 16. サウンド

**v1 では実装しない。** v1.1 で WebAudio API による効果音（ジャンプ / 踏みつけ / 死亡 / チェックポイント / ゴール）を追加する。

---

## 17. 非機能要件

- **60fps 目標。** 固定タイムステップ（1/60秒）のアキュムレータ方式でロジックを更新し、描画は毎フレーム1回。
- 外部依存ゼロ・ビルド不要。
- ES Modules を使用するため、ローカル確認には HTTP サーバが必要（`python -m http.server`）。
- **全ソースコードのコメントは日本語で書く。**
- 1ファイルは原則 300行以内。超える場合は分割を検討する。

---

## 18. ディレクトリ構成

```
side-scroller-game/
├── index.html
├── style.css
├── SPEC.md              ← 本ファイル
├── TASKS.md             ← 実装単位の分解表
├── README.md
├── prompts/             ← 実装担当(Sonnet)向けプロンプト
└── src/
    ├── main.js          エントリポイント / ゲームループ / 画面遷移
    ├── config.js        全パラメータ定数
    ├── input.js         キーボード + タッチ入力
    ├── goat.js          ヤギの状態機械と物理
    ├── goatRenderer.js  ヤギのプロシージャル描画
    ├── world.js         地形データの解釈と衝突判定
    ├── camera.js        カメラ追従
    ├── background.js    パララックス背景
    ├── enemies.js       敵の挙動と描画
    ├── hud.js           HUD / メニュー画面
    ├── storage.js       localStorage 進捗保存
    └── stages/
        ├── stage1.js
        ├── stage2.js
        └── stage3.js
```

---

## 19. 公開

GitHub リポジトリを作成し、`main` ブランチのルートを GitHub Pages で公開する。
公開URL: `https://<ユーザー名>.github.io/<リポジトリ名>/`
